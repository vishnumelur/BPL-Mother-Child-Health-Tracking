// src/db/seed.ts — stub. Full implementation lands in Phase 11.
export async function runSeed() {
  console.warn("runSeed() stub — full seed lands in Phase 11");
  return { seedVersion: "stub", note: "Phase 11 not yet executed" };
}
